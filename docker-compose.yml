version: "3.9"

services:
  bot:
    build: .
    container_name: appointment_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/bot/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
